# تقرير إصلاح المشاكل - LinkHub

## تاريخ الإصلاح
13 يوليو 2026

## المشاكل التي تم حلها

### 1. مشكلة تزامن أزرار الاستئناف/الإيقاف
**الوصف**: الأزرار في الجدول الكامل (صفحة التحميلات) والجدول المصغر (الصفحة الرئيسية) كانت تعمل بشكل مستقل بدون تزامن، مما يؤدي إلى اختلاف في الحالات بين الجدولين.

**السبب**: كل زر كان يحدث حالته بشكل منفرد بدون آلية تزامن بين الجدولين.

**الحل**: 
- إنشاء دالة موحدة `syncStopButtonState()` في `downloadManager.js` تقوم بتحديث حالة الزر في كلا الجدولين بشكل متزامن
- تحديث جميع معالجات الأحداث لاستخدام هذه الدالة بدلاً من التحديث المباشر

**الملفات المعدلة**:
- `src/renderer/js/ui/downloadManager.js`

---

### 2. مشكلة عدم تغير حالة زر الاستئناف إلى إيقاف عند بدء التحميل
**الوصف**: عند الضغط على زر "استئناف"، يتحول النص إلى "جاري الاستئناف..." لكن عند بدء التحميل الفعلي لا يتغير الزر إلى "إيقاف".

**السبب**: عدم وجود حدث يُشعر الواجهة بأن التحميل قد بدأ فعلياً بعد الاستئناف.

**الحل**:
- إضافة حدث `onDownloadResumed` في `DownloadStateSyncService` لإرسال إشعار عند استئناف التحميل
- إضافة معالج الحدث في `eventService.js` 
- إضافة معالج `onResumed` في `main.js` لتحديث حالة الزر فوراً عند استئناف التحميل

**الملفات المعدلة**:
- `src/main/infrastructure/sync/DownloadStateSyncService.js`
- `src/renderer/js/services/eventService.js`
- `src/renderer/js/main.js`

---

### 3. مشكلة تسمية الملفات بالطابع الزمني
**الوصف**: النظام كان يضيف timestamp لاسم الملف لضمان التفرد، لكن هذا يمنع yt-dlp من التعرف على الملفات السابقة واستئناف التحميل من حيث توقف.

**السبب**: في `YtdlpAdapter.js` السطور 102-105، كان يتم استخدام `Date.now()` في اسم الملف مما يجعل كل تحميل له اسم فريد مختلف.

**الحل**:
- تغيير استراتيجية التسمية لاستخدام اسم الملف الفعلي القادم من yt-dlp (title)
- إذا لم يتوفر title، استخدام hash ثابت من URL (MD5) لضمان ثبات الاسم لنفس الرابط
- إضافة دالة `createHashFromUrl()` لإنشاء hash ثابت من URL
- استيراد `sanitizeFileName` من `YtdlpUtils` لتنظيف اسم الملف

**الملفات المعدلة**:
- `src/main/infrastructure/media/YtdlpAdapter.js`

---

## النتائج

- **تزامن الأزرار**: الأزرار في الجدولين تعمل الآن كزر واحد متطابق تماماً
- **تحديث الحالة**: زر الاستئناف يتغير فوراً إلى "إيقاف" عند بدء التحميل
- **استئناف التحميل**: النظام يمكنه الآن التعرف على الملفات السابقة واستئناف التحميل من حيث توقف
- **استخدام الاسم الفعلي**: الملفات تُسمى بالاسم الفعلي من يوتيوب مما يسهل التعرف عليها

## التحقق

تم التحقق من أن جميع التعديلات:
1. متوافقة مع البنية الحالية للكود
2. تحافظ على التوافق مع yt-dlp
3. لا تؤثر على الوظائف الأخرى
4. تتبع أفضل الممارسات البرمجية
